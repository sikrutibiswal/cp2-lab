//How can you implement a program to count the number of vowels in a string
#include<stdio.h>
#include<string.h>
{
	char temp[100];
	 int c=0;
	 int i;
	 printf("enter a string")
	 scanf("%s",temp);
	 for(i=0;temp[i]!=0;i++)
	 if(temp[i])
}
